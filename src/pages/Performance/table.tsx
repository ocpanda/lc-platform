import { SearchForm } from './useSearch'

interface Props {
  form: FormState<SearchForm, string | string[] | undefined>
}

function TableComp(props: Props): JSX.Element {
  return <div></div>
}
export default TableComp
